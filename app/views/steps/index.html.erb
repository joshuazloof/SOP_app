<h1>Listing steps</h1>

<table>
  <tr>
    <th>Ordernum</th>
    <th>Instruction</th>
    <th>Timing</th>
    <th>Tip</th>
    <th>Iscurrent</th>
    <th>sop_id</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @steps.each do |step| %>
  <tr>
    <td><%= step.orderNum %></td>
    <td><%= step.instruction %></td>
    <td><%= step.timing %></td>
    <td><%= step.tip %></td>
    <td><%= step.isCurrent %></td>
    <td><%= step.sop_id %></td>
    <td><%= link_to 'Show', step %></td>
    <td><%= link_to 'Edit', edit_step_path(step) %></td>
    <td><%= link_to 'Destroy', step, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Step', new_step_path %>
